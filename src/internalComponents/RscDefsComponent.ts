import RscDOMComponent from './RscDOMComponent'

export default class RscDefsComponent extends RscDOMComponent {
  draw() {
    const ctx = this.ctx
    const element = this._currentElement
    // console.log('RscDefsComponent#draw()')

    // todo LAST EDIT HERE

    // ctx.enterDefsMode()
    // processProps(ctx, element)
    // drawing.draw(ctx, element)
    // this._renderedChildren.forEach(child => child.draw())
    // ctx.exitDefsMode()
  }
}
